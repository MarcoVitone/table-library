const CollapsedComponent = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "100%",
        minHeight: "2.5rem",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    />
  );
};

export default CollapsedComponent;
